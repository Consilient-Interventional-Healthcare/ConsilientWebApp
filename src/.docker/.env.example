# Docker Compose Environment Variables
# Copy this file to .env and fill in the values

# Database
SA_PASSWORD=YourStrong!Passw0rd

# Grafana
GRAFANA_ADMIN_PASSWORD=admin

# Ollama WebUI
OLLAMA_WEBUI_SECRET_KEY=your-secret-key-here

# Development HTTPS Certificates
# All services (WebApp2, API, BackgroundHost) share certificates from src/.docker/certs/
#
# Setup (one time):
# 1. Install mkcert: https://github.com/FiloSottile/mkcert
#    Windows: choco install mkcert
#    Mac: brew install mkcert
#    Linux: See GitHub page
#
# 2. Trust the CA (first time only):
#    mkcert -install
#
# 3. Generate certificates:
#    mkcert -cert-file src/.docker/certs/localhost.pem -key-file src/.docker/certs/localhost-key.pem localhost 127.0.0.1

# Docker Registry (optional)
# DOCKER_REGISTRY=
