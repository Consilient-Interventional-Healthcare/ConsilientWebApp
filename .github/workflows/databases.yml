name: Deploy Databases

on:
  workflow_call:
    inputs:
      environment:
        description: 'Environment to deploy to (dev or prod)'
        required: true
        type: string
      databases:
        description: 'JSON array of database directory names (from centralized discovery)'
        required: true
        type: string
      recreate-database-objects:
        description: 'Recreate ALL database objects (DELETE ALL DATA - dev only)'
        required: false
        type: boolean
        default: false
      azure-sql-server-fqdn:
        description: 'Azure SQL Server FQDN (fully qualified domain name)'
        required: true
        type: string
      runner_image:
        description: 'Custom runner container image with sqlcmd and other tools pre-installed'
        required: true
        type: string
    secrets:
      ARM_CLIENT_ID:
        required: true
        description: 'Azure Service Principal Client ID for OIDC authentication'
      ARM_TENANT_ID:
        required: true
        description: 'Azure Tenant ID for OIDC authentication'
      AZURE_SUBSCRIPTION_ID:
        required: true
        description: 'Azure Subscription ID for OIDC authentication'
      ARM_CLIENT_SECRET:
        required: false
        description: 'Azure Client Secret for local testing with act (fallback)'

  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment to deploy to'
        required: true
        type: choice
        options:
          - dev
          - prod
        default: 'dev'
      recreate-database-objects:
        description: 'Recreate ALL database objects (DELETE ALL DATA - dev only)'
        required: false
        type: boolean
        default: false

permissions:
  contents: read
  id-token: write

jobs:

  validate-inputs:
    name: Validate Inputs
    runs-on: ubuntu-latest
    timeout-minutes: 5
    env:
      AZURE_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
      ARM_CLIENT_ID: ${{ secrets.ARM_CLIENT_ID }}
      ARM_CLIENT_SECRET: ${{ secrets.ARM_CLIENT_SECRET }}
      ARM_TENANT_ID: ${{ secrets.ARM_TENANT_ID }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Validate environment input and secrets
        uses: ./.github/actions/validate-inputs
        with:
          environment: ${{ inputs.environment }}
          required-secrets: |
            [
              {"name": "AZURE_SUBSCRIPTION_ID", "env": "AZURE_SUBSCRIPTION_ID", "description": "Azure subscription"},
              {"name": "ARM_CLIENT_ID", "env": "ARM_CLIENT_ID", "description": "Azure authentication"},
              {"name": "ARM_CLIENT_SECRET", "env": "ARM_CLIENT_SECRET", "description": "Azure authentication"},
              {"name": "ARM_TENANT_ID", "env": "ARM_TENANT_ID", "description": "Azure authentication"}
            ]

      - name: Summary
        run: |
          echo "### Input Validation Complete ✅" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Environment:** \`${{ inputs.environment }}\`" >> $GITHUB_STEP_SUMMARY
          echo "**Databases Count:** \`$(echo '${{ inputs.databases }}' | jq -r 'length' 2>/dev/null || echo 'N/A')\`" >> $GITHUB_STEP_SUMMARY
          echo "**SQL Server:** \`${{ inputs.azure-sql-server-fqdn }}\`" >> $GITHUB_STEP_SUMMARY
          echo "**Recreate Objects:** \`${{ inputs.recreate-database-objects }}\`" >> $GITHUB_STEP_SUMMARY

  deploy-database:
    name: Deploy ${{ matrix.database }} (${{ inputs.environment }})
    needs: validate-inputs
    runs-on: ubuntu-latest
    container:
      image: ${{ inputs.runner_image }}
      credentials:
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}
    timeout-minutes: 30
    if: inputs.databases != '[]'
    permissions:
      contents: read
      id-token: write
      packages: read
    strategy:
      matrix:
        database: ${{ fromJson(inputs.databases) }}
      fail-fast: false
    concurrency:
      group: deploy-database-${{ inputs.environment }}-${{ matrix.database }}
      cancel-in-progress: false

    env:
      SQL_SERVER: ${{ inputs.azure-sql-server-fqdn }}
      RECREATE_DB: ${{ inputs.recreate-database-objects || 'false' }}
      ENVIRONMENT: ${{ inputs.environment }}
      DATABASE_BASE_NAME: ${{ matrix.database }}
      DATABASE_NAME: ${{ matrix.database }}_${{ inputs.environment }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Login to Azure (OIDC + act fallback)
        uses: ./.github/actions/azure-login
        with:
          client-id: ${{ secrets.ARM_CLIENT_ID }}
          tenant-id: ${{ secrets.ARM_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
          client-secret: ${{ secrets.ARM_CLIENT_SECRET || '' }}

      - name: Validate required secrets
        run: |
          echo "Validating required configuration..."

          if [ -z "$SQL_SERVER" ]; then
            echo "❌ Error: ARM_SQL_SERVER secret is not set"
            exit 1
          fi

          echo "✅ Using Azure AD Authentication"
          echo "✅ SQL Server: ${SQL_SERVER}"
          echo "✅ Database: ${DATABASE_NAME}"

          echo "::debug::Environment Variables - ENVIRONMENT: ${ENVIRONMENT}, DATABASE_BASE_NAME: ${DATABASE_BASE_NAME}, RECREATE_DB: ${RECREATE_DB}"

      - name: Database connectivity health check
        uses: ./.github/actions/health-check-database
        with:
          sql-server: ${{ env.SQL_SERVER }}
          database-name: ${{ env.DATABASE_NAME }}
          timeout: '15'

      - name: List SQL files to apply
        id: list-files
        run: |
          set -e

          echo "📄 SQL files in ${{ env.DATABASE_BASE_NAME }} directory:"
          echo "   Target database: ${{ env.DATABASE_NAME }}"

          # Use relative path to database scripts directory (works in both host and container)
          SCRIPTS_DIR="src/Databases/${{ env.DATABASE_BASE_NAME }}"

          echo "::debug::Script Directory - Full path: $SCRIPTS_DIR, Database: ${{ env.DATABASE_BASE_NAME }}"

          # Verify directory exists
          if [ ! -d "$SCRIPTS_DIR" ]; then
            echo "❌ ERROR: Database directory not found: $SCRIPTS_DIR"
            exit 1
          fi

          cd "$SCRIPTS_DIR"

          echo "::debug::Directory contents:" && ls -lah 2>&1 | while IFS= read -r line; do echo "::debug::  $line"; done

          echo "   Files to execute:"
          SQL_FILES=$(find . -maxdepth 1 -type f -name "*.sql" | sed 's|^\./||' | sort)

          # Filter out *seed.sql files if not recreating database objects
          if [ "${{ env.RECREATE_DB }}" != "true" ]; then
            SQL_FILES=$(echo "$SQL_FILES" | grep -v "seed\.sql$" || true)
          fi

          if [ -n "$SQL_FILES" ]; then
            echo "$SQL_FILES" | sed 's/^/   - /'
          else
            echo "   No SQL files found (or all filtered out)"
          fi
          echo ""

      - name: Prevent destructive actions in non-dev
        run: |
          if [ "$RECREATE_DB" = "true" ] && [ "$ENVIRONMENT" != "dev" ]; then
            echo "❌ Recreate database is only allowed in the dev environment."
            exit 1
          fi

      - name: Recreate all database objects (dev only)
        if: env.RECREATE_DB == 'true' && env.ENVIRONMENT == 'dev'
        uses: ./.github/actions/sqlcmd-execute
        with:
          sql-server: ${{ env.SQL_SERVER }}
          database-name: ${{ env.DATABASE_NAME }}
          script-file: "infra/db/drop_all_objects.sql"
          timeout: '300'
          fail-on-error: 'true'

      - name: Apply SQL scripts
        shell: bash
        run: |
          set -e
          set -o pipefail

          # Helper function to execute sqlcmd with Azure AD authentication
          run_sqlcmd() {
            local script_file="$1"
            timeout 600 sqlcmd -S "${SQL_SERVER}" \
              -d "${DATABASE_NAME}" \
              -G \
              -i "$script_file" \
              -b 2>sqlcmd_error.log || return $?
          }

          # Use relative path to database scripts directory (works in both host and container)
          SCRIPTS_DIR="src/Databases/${{ env.DATABASE_BASE_NAME }}"

          # Verify directory exists
          if [ ! -d "$SCRIPTS_DIR" ]; then
            echo "❌ ERROR: Database directory not found: $SCRIPTS_DIR"
            exit 1
          fi

          cd "$SCRIPTS_DIR"

          # Get all .sql files sorted alphabetically (portable for both Alpine and Ubuntu)
          SQL_FILES=$(find . -maxdepth 1 -type f -name "*.sql" | sed 's|^\./||' | sort)

          # Filter out *seed.sql files if not recreating database objects
          if [ "$RECREATE_DB" != "true" ]; then
            SQL_FILES=$(echo "$SQL_FILES" | grep -v "seed\.sql$" || true)
          fi

          if [ -z "$SQL_FILES" ]; then
            echo "⚠️  No SQL files found in ${{ env.DATABASE_BASE_NAME }} directory"
            exit 0
          fi

          # Apply each SQL file in order
          FILE_COUNT=$(echo "$SQL_FILES" | wc -l)
          FILE_NUM=1

          echo "🚀 Deploying $FILE_COUNT SQL script(s) to ${{ env.DATABASE_NAME }}"
          echo ""

          while IFS= read -r sql_file; do
            [ -z "$sql_file" ] && continue

            echo "================================================"
            echo "📝 Applying [$FILE_NUM/$FILE_COUNT]: $sql_file"
            echo "================================================"

            echo "::debug::Execution details - Server: ${SQL_SERVER}, Database: ${DATABASE_NAME}, Script: $sql_file, Auth: Azure AD (-G flag), Timeout: 600 seconds"

            run_sqlcmd "$sql_file"
            exit_code=$?

            if [ $exit_code -eq 0 ]; then
              echo "✅ $sql_file completed successfully"
            elif [ $exit_code -eq 124 ]; then
              echo "❌ $sql_file timed out after 600 seconds"
              echo "--- sqlcmd error output ---"
              cat sqlcmd_error.log 2>/dev/null || echo "No error log available"
              echo "--------------------------"
              exit 1
            else
              echo "❌ $sql_file failed with exit code $exit_code"
              echo "--- sqlcmd error output ---"
              cat sqlcmd_error.log 2>/dev/null || echo "No error log available"
              echo "--------------------------"
              exit 1
            fi

            FILE_NUM=$((FILE_NUM + 1))
            echo ""
          done <<< "$SQL_FILES"

          echo "================================================"
          echo "✅ All SQL scripts deployed successfully"
          echo "   Database: ${{ env.DATABASE_NAME }}"
          echo "   Scripts: $FILE_COUNT"
          echo "================================================"

      - name: Verify database deployment
        shell: bash
        run: |
          set -e

          # Helper function to execute sqlcmd with Azure AD authentication
          run_sqlcmd_verify() {
            local script_file="$1"
            timeout 60 sqlcmd -S "${SQL_SERVER}" -d "${DATABASE_NAME}" -G -i "$script_file" -b 2>/dev/null
          }

          echo ""
          echo "================================================"
          echo "Verifying database: ${{ env.DATABASE_NAME }}"
          echo "================================================"

          # Validate required SQL scripts exist and are executable
          VERIFICATION_FAILED=false

          if [ ! -f "infra/db/list_tables.sql" ]; then
            echo "❌ ERROR: infra/db/list_tables.sql not found"
            VERIFICATION_FAILED=true
          else
            echo "Tables in database:"
            if run_sqlcmd_verify "infra/db/list_tables.sql"; then
              echo "✅ Table listing completed"
            else
              echo "❌ ERROR: Could not list tables - database may be inaccessible or corrupt"
              VERIFICATION_FAILED=true
            fi
          fi

          echo ""
          if [ ! -f "infra/db/table_count.sql" ]; then
            echo "⚠️  Warning: infra/db/table_count.sql not found, skipping table count"
          else
            echo "Database size:"
            if run_sqlcmd_verify "infra/db/table_count.sql"; then
              echo "✅ Table count completed"
            else
              echo "⚠️  Could not get table count (optional check)"
            fi
          fi

          echo ""
          if [ "$VERIFICATION_FAILED" = "true" ]; then
            echo "❌ DEPLOYMENT FAILED: Database verification failed"
            exit 1
          fi

          echo "✅ Database ${{ env.DATABASE_NAME }} deployment completed successfully!"

  summary:
    name: Deployment Summary
    runs-on: ubuntu-latest
    timeout-minutes: 5
    needs: [validate-inputs, deploy-database]
    if: always()
    steps:
      - name: Summary
        run: |
          ENVIRONMENT="${{ inputs.environment }}"
          DATABASES='${{ inputs.databases }}'
          DEPLOY_RESULT='${{ needs.deploy-database.result }}'
          RECREATE_OBJECTS="${{ inputs.recreate-database-objects }}"
          SQL_SERVER="${{ inputs.azure-sql-server-fqdn }}"

          echo "### Database Deployment Complete 🗄️" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          # Deployment Configuration
          echo "**Configuration:**" >> $GITHUB_STEP_SUMMARY
          echo "- **Environment**: \`$ENVIRONMENT\`" >> $GITHUB_STEP_SUMMARY
          echo "- **SQL Server**: \`$SQL_SERVER\`" >> $GITHUB_STEP_SUMMARY
          echo "- **Recreate Objects**: $RECREATE_OBJECTS" >> $GITHUB_STEP_SUMMARY

          if [ "${{ github.event_name }}" != "workflow_call" ]; then
            echo "- **Commit**: \`${{ github.sha }}\`" >> $GITHUB_STEP_SUMMARY
            echo "- **Triggered by**: ${{ github.actor }}" >> $GITHUB_STEP_SUMMARY
          fi
          echo "" >> $GITHUB_STEP_SUMMARY

          # Show actual database names deployed
          echo "**Databases Deployed:**" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          if [ "$DATABASES" != "[]" ] && [ -n "$DATABASES" ]; then
            DB_COUNT=$(echo "$DATABASES" | jq -r '. | length')
            echo "_Total: $DB_COUNT database(s)_" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            while IFS= read -r db; do
              echo "- \`${db}_${ENVIRONMENT}\`" >> $GITHUB_STEP_SUMMARY
            done < <(echo "$DATABASES" | jq -r '.[]')
          else
            echo "- _No databases provided for deployment_" >> $GITHUB_STEP_SUMMARY
          fi

          echo "" >> $GITHUB_STEP_SUMMARY

          # Overall status
          if [ "$DATABASES" == "[]" ] || [ -z "$DATABASES" ]; then
            echo "⚠️ **Status**: No databases to deploy" >> $GITHUB_STEP_SUMMARY
          elif [ "$DEPLOY_RESULT" == "success" ]; then
            echo "✅ **Status**: All databases deployed successfully!" >> $GITHUB_STEP_SUMMARY
          elif [ "$DEPLOY_RESULT" == "failure" ]; then
            echo "❌ **Status**: SQL script deployment failed for one or more databases" >> $GITHUB_STEP_SUMMARY
          else
            echo "⚠️ **Status**: Deployment $DEPLOY_RESULT" >> $GITHUB_STEP_SUMMARY
          fi
