# Act configuration file
# This file configures the local GitHub Actions emulator (act)

# Use the custom runner image for ubuntu-latest
-P ubuntu-latest=consilientwebapp-runner:latest

# Bind the current directory (mount volumes)
--bind

# Don't pull the image (use local image)
--pull=false

# Load environment variables from .env file to simulate GitHub Variables
--env-file infra/github_emulator/.env
